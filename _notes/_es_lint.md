# es lint config

create file in root `jsconfig.json`

```json
{
    "compilerOptions": {
        "target": "es6",
        "module": "node16",
        "explainFiles": true
    },
    "exclude": ["node_modules"]
}
```

no more warnings
